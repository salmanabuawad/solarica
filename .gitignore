__pycache__/
*.pyc
node_modules/
.vite/
.venv/
*.egg-info/
dist/
.env

# Claude Code scratch state (local only)
.claude/

# Per-project parsed artefacts + uploaded design files live in the data
# tree; they can be very large and may contain customer-supplied PDFs.
backend/data/
backend/uploads/
data/

# Local diagnostic renders — regenerated by scripts, never meant for git
aligned_block_mapping.png
blocks_overlay.png
boundary_mask.png
block_mapping_input.png
