"""EPL — Execution Planning Layer.

Phase 2 of the roadmap. The currently-deployed routes in
`backend/app/main.py` (project CRUD, upload, parse, blocks/trackers/piers,
plant-info, pier-status) are the EPL module. When the module migration
happens they move verbatim here under `routes.py`.
"""
